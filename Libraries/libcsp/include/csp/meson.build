configure_file(
  format : 'cmake@',
  input : 'csp_autoconfig.h.in',
  output : 'csp_autoconfig.h',
  configuration : csp_config,
  install : true,
  install_dir : get_option('includedir') / 'csp',
)
